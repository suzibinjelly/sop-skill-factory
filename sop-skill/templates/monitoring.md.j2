---
name: {{ name }}
description: {{ description }}
---

# {{ name }}

> 类型：monitoring（监控运维型）

## 概述

{{ overview }}

{% if inputs %}
## 输入参数

| 参数名 | 类型 | 必填 | 说明 |
| ------ | ---- | ---- | ---- |
{% for p in inputs %}
| {{ p.param_name }} | {{ p.type }} | {{ p.required }} | {{ p.description }} |
{% endfor %}
{% endif %}

## 监控指标

| 指标 | 正常阈值 | 告警阈值 | 严重阈值 |
| ---- | -------- | -------- | -------- |
{% for m in metrics %}
| {{ m.name }} | {{ m.threshold_normal }} | {{ m.threshold_warning }} | {{ m.threshold_critical }} |
{% endfor %}

## 处理动作

{{ actions }}

## 升级路径

{{ escalation_path }}

## 约束条件

{% for c in constraints %}
- **{{ c.constraint }}**：{{ c.reason }}
{% endfor %}

## 质量验证

| 检查项 | 验证方法 | 通过标准 |
| ------ | -------- | -------- |
{% for q in quality_gates %}
| {{ q.check_item }} | {{ q.method }} | {{ q.pass_criteria }} |
{% endfor %}

## 参考来源

| 要素 | 来源文件 | 来源章节 |
| ---- | -------- | -------- |
{% for s in sources %}
| {{ s.element }} | {{ s.source_file }} | {{ s.source_section }} |
{% endfor %}
